apiVersion: v1
kind: Service
metadata:
  name: hospitality-service
spec:
  type: NodePort
  selector:
    app: hospitality
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
